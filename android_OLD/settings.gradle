pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "outlet_app"
include ':app'

// ✅ Add missing Flutter plugin includes
def flutterProjectRoot = rootProject.projectDir.parentFile
apply from: "$flutterProjectRoot/.flutter-plugins"
apply from: "$flutterProjectRoot/.flutter-plugins-dependencies"
